its a gen ai project
where user uploads his resume and targeted job discriptions,
then this project help us to find the gap in the resume and if user 
wants it can also genarates a ATS friendly resume for the user.


steps:
    1. We make a server, and then setup user auth API's 
            (npm init -y)  -nodeJS application initiated
        for making server 
            npm i express mongoose dotenv
    2. We setup database COnfig and created database.js 
    3. Then we created schema for user model
    4. and then we creating routes for authentication
    5. then we make controllers 
        auth.controller.js -> it is for taking care of user register and login
we need some more packages like -> bcryptjs jsonwebtoken cookie-parser(token to cookie, cookie to read)
                now we have login and register is ready so now we will work on logout
                    -> for logout we are doing token blacklisting(with mongodb, in high end apps we use reddish)
        created a getme route and its middleware



It's time for Frontend -->

        > npm create vite@latest .         (for creating the latest app)
        
        for importing and using SCSS files
        > npm i sass

        for navigation we need one more package
        > npm i react-router

now we setup a login and a register route.....

                    


in react we follow 4 layer architecture

1-> UI 
    => components
    => pages

2-> Hook      -> for managing state and API layers
    => hooks

3-> State   -> used for storing the data
    => auth.context.jsx  (manage user and loading state)
    => ai.context.jsx

4-> API    -> for communication with backend
=> services
    => auth.api.js


for communication with backend, Frontend needs a package called -> axios
        > npm i axios


        >npm i cors

after it we did cors setup 

perform two way binding in login and registration form 

then we used a useEffect hook in useAuth.js hook, so getMe can work and fetch users data in every refresh.



-> its time to add AI features
1. at first we make interview model schema
2. then we install gemini
    > npm install @google/genai

    you can get google-gemini-api from "google ai studio"


    now we check that is our model working or not, 

Now we make a temp.js which consists sample data

    now we need to get structured output
    > npm i zod
    > npm i zod-to-json-schema 
    by this we created the structured output from the AI
        we create 'ai.services.js'
        the interview.routes.js and interview.controller.js

now for uploading the pdf we need a package
    > npm i multer
for reading the pdf's data we need
    > npm i pdf-parse

now our API is working fine 

now we will make the frontend 
and make a interview features -> we will make pages, services and styles
and change the protected homepage heading to the home.jsx inside pages

then we build the ui fo rhome page , interview .jsx page 
and we make the four layer react architecture
for both ui

now we are adding one more feature so AI will make the required resume...

Puppeter -> its help to enable browsers feature in server side environment
and in browser we have a option of create pdf from html,

now we will genrate html from ai 
then we give this html to our puppeter AI
and this package will help us to make pdf